// funzione che permette di inserire i dati e associare tali infomrazioni a delle variabili
const nomeUser = prompt("inserisci il tuo nome");
const cognomeUser = prompt("inserisci il tuo cognome");
const colore = prompt("quale è il tuo colore preferito?");

let = nomeUser, cognomeUser, colore;

console.log(let);

// Stampo il risultato in pagina

document.getElementById("risultato").innerHTML = "La tu password sarà: " +nomeUser +cognomeUser +colore  + "23";
